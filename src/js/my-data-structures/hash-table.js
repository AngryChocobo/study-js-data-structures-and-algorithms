const { defaultToString } = require('../utils/index');

class HashTable {
    constructor(toStrFn = defaultToString) {
        this.toStrFn = toStrFn;
        this.table = {};
    }

    put(element) {

    }

    remove(element) {

    }

    get(element) {

    }
}